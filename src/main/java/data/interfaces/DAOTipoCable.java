package data.interfaces;

import models.TipoCable;

/**
 * DAOTipoCable interface that extends the generic CRUD interface for TipoCable objects.
 */
public interface DAOTipoCable extends CRUD<TipoCable> {
}